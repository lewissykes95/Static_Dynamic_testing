class CardGame:

   def check_for_ace(self, card):
    if card.value = 1:
      return True
    else
      return False

# the equals operator should be "=="
# the else should have a semi-colon after it

  
  dif highest_card(self, card1 card2):
  if card1.value > card2.value:
    return card
  else:
    return card2


# there should be a comma after the card1 parameter
# to define a function we use 'def' not 'dif'
# the 'if' should be indented
  

def cards_total(self, cards):
  total = 
  for card in cards:
    total += card.value
    return "You have a total of" + total


# the 'cards_total' function should be indented
# total has an 'equals' sign but no value
# the return should not be indented


  